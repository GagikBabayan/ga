#include <iostream>
#include "GraphList.hpp"

using namespace std;

int main()
{

  Graph *graph = new Graph(5);

  graph->addEdge(0, 1);
  graph->addEdge(1, 2);
  graph->addEdge(1, 3);
  graph->addEdge(2, 3);
  graph->print();
  return 0;
}
