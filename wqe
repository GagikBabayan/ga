#include "hashtable.hpp"

int main()
{

  int a[] = {15, 11, 27, 8, 12};
  int n = sizeof(a) / sizeof(a[0]);

  string b[] = {"hello", "world", "Armenia"};
  int k = sizeof(b) / sizeof(b[0]);

  Hash<string> h(3);

  /*for (int i = 0; i < n; i++)
  {
    h.insertItem(a[i], i);
  }
  */
  h.insertItem("hello", 3);
  h.displayHash();

  return 0;
}
